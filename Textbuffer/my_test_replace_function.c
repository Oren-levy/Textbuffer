////////////////////////////////////////////////////////////////////////
// COMP2521 19T0 ... assignment 1: Textbuffer ADT submission tests

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "textbuffer.h"
#define TRUE 1
#define FALSE 0
int main (void)
{
#define TEST_1 "line1\nline2\nline3\nline4\nline5\n"
#define TEST_2	"line5\nline6\nline7\n"

   Textbuffer tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line1", "I");
   textbuffer_drop (tb);
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line2", "Love");
   textbuffer_drop (tb);
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line3", "to");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line4", "code");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line5", "all night long");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line1", "");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line4", "");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line5", "");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line1", "         ");
   textbuffer_drop (tb);
      /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   /////////////////////////////////////////////////////////////////////
   tb = textbuffer_new (TEST_1);
   assert (tb != NULL);
	//char *match;
   textbuffer_replace (tb, "line0", "Love");
   textbuffer_drop (tb);
   
return EXIT_SUCCESS;
}

