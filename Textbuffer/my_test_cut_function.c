////////////////////////////////////////////////////////////////////////
// COMP2521 19T0 ... assignment 1: Textbuffer ADT submission tests

#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "textbuffer.h"

int main (void)
{
#define TEST_1 "line1\nline2\nline3\nline4\nline5\n"
#define TEST_2	"line6\nline7\n"

	Textbuffer tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 0, 1);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 0, 2);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 0, 3);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 0, 4);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 0, 5);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 1, 2);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 1, 3);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 1, 4);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 4, 4);
    
    textbuffer_drop (tb);
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    ////////////////////////////////////////////////////////////////////
    tb = textbuffer_new (TEST_1);
	assert (tb != NULL);

    textbuffer_cut (tb, 0, 0);
    
    textbuffer_drop (tb);
	return EXIT_SUCCESS;
}
